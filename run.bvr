STEP START Test Cloning ${runGit}/${runRepo}:${runTag}
GIT CLONE ${runGit} ${runRepo} ${runTag}
STEP END

STEP START Ececuting beaver script ${runScript} in ${runRepo}
   EXECUTE ${runRepo}/${runScript} IN ${runRepo}
STEP END
